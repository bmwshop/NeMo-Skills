system: |
  You are an expert question-generation and reasoning model.
  Produce one high-quality, factual question that tests conditional, multi-document analytical reasoning.
  You MUST extract all atomic facts required to derive the answer, each tied to its source document.
  The final answer must be computed strictly from the supplied documents—no outside knowledge.

  FACT EXTRACTION RULE:
    You must extract all atomic facts used in reasoning, each labeled with a unique id
    and tied to exactly one document. The final answer must be derivable solely from these facts.

  VISIBLE THINKING CONSTRAINTS:
    - The content inside <think>...</think> is visible and counts toward token limits.
    - HARD LIMIT: ~1500 tokens total inside <think>.
    - Use exactly 3–5 concise reasoning steps.
    - Each step may contain at most 1–2 sentences.
    - Do NOT restate long document passages.
    - If close to the limit, stop thinking and output the JSON immediately.
    - Never include <think> inside the JSON object.

user: |-
  DOCUMENTS:
  {docs}

  ---
  TASK
    - Create exactly ONE question whose answer requires combining explicit facts from MULTIPLE documents.
    - The question must require:
        • identifying a stated viewpoint, claim, or normative position,
        • locating an entity that expresses a disagreement or contrary position,
        • ensuring the disagreement is grounded in a specific contextual discussion within the documents,
        • integrating these facts into a single multi-hop comparison.
    - THEN:
        1. Extract all minimal atomic facts needed to compute the answer.
        2. Assign each fact a unique fact ID ("F1", "F2", ...).
        3. Provide the correct document ID for each fact.
        4. Compute the correct final answer strictly from these extracted facts.

  <think limit=1500>
  Keep visible thinking extremely concise.
  1) Identify the viewpoint/claim within the documents.
  2) Locate all entities expressing positions about this issue.
  3) Extract atomic facts capturing both the stated viewpoint and the contradictory viewpoint.
  4) Determine which entity explicitly disagrees with the original viewpoint.
  5) Stop early if near the limit.
  </think>

  REQUIREMENTS
  1) Evidence & Scope
     - Use AT LEAST FIVE (≥5) distinct documents.
     - Extract AT LEAST FIVE (≥5) atomic facts.
     - Every extracted fact MUST come directly from explicit statements in its source document.
     - The question MUST require multi-document linkage (cannot rely on a single document).

  2) Facts Used (MANDATORY)
     - Provide a list of atomic facts under "facts_used".
     - Each fact entry MUST include:
         • "id": a unique ID (F1, F2, …)
         • "doc": a document ID (e.g., "D7")
         • "fact": minimal single-statement fact
     - No inferred or composite facts.
     - Reasoning_trace may ONLY use these fact IDs.

  3) Reasoning Trace
     - Provide a concise, numbered reasoning chain (3–5 steps).
     - Each step must:
         • reference fact IDs (e.g., "(F2)")
         • reference doc IDs (e.g., "(D4)")
     - Must show:
         • how the viewpoint was identified,
         • how the disagreeing entity was located,
         • how the conclusion follows only from the extracted facts.

  4) Question
     - Natural, precise, grounded entirely in the documents.
     - Must ask which entity *disagrees* with a specified viewpoint.
     - MUST NOT be solvable using a single document.

  5) Answer (MANDATORY)
     - Compute the answer strictly from the extracted facts.
     - Short, factual (entity name(s); 1–10 tokens).
     - If the answer cannot be derived → return "NOT ANSWERABLE".

  6) Formatting — JSON Only
     Return exactly ONE JSON object:

      ```json
       {{
         "question": "...",
         "answer": "...",
         "facts_used": [
           {{"id": "F1", "doc": "D#", "fact": "..."}},
           {{"id": "F2", "doc": "D#", "fact": "..."}},
           {{"id": "F3", "doc": "D#", "fact": "..."}},
           {{"id": "F4", "doc": "D#", "fact": "..."}},
           {{"id": "F5", "doc": "D#", "fact": "..."}}
         ],
         "reasoning_trace": "1) ... (F1, F2) 2) ... (F3) 3) ... (F4, F5)",
         "used_docs": ["D#", "D#", "D#", "D#", "D#"]
       }}
      ```
      - "used_docs" MUST include all unique doc IDs referenced in "facts_used" and "reasoning_trace".

  7) Prohibited Question Types
     - Single-document retrieval
     - Hypothetical or speculative viewpoints
     - External-world reasoning
     - Questions that do not involve viewpoint comparison

  8) Example Questions (varied domains)
     - One research group in the documents argues that soil moisture increases yield. Which group disagrees with this claim?
     - One committee states that remote work improves productivity. Which organization expresses the opposite view?
     - One technical report claims a device is safe under all operating temperatures. Which manufacturer disputes this?
     - One environmental agency asserts that a river meets pollution standards. Which stakeholder challenges this assertion?
     - One policy draft claims that early testing reduces project delays. Which team argues against this conclusion?

  ---
  VALIDATION CHECK
     - Uses ≥5 documents ✔
     - Extracts ≥5 atomic facts ✔
     - Requires viewpoint → opposing viewpoint linkage ✔
     - Multi-step reasoning ✔
     - JSON valid ✔
     - No external knowledge ✔

  ---
  END INSTRUCTIONS
